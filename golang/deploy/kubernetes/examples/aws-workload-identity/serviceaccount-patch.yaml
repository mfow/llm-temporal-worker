apiVersion: v1
kind: ServiceAccount
metadata:
  name: llmtw-worker
  annotations:
    # Replace with the reviewed IAM role ARN; do not commit a tenant role.
    eks.amazonaws.com/role-arn: arn:aws:iam::REPLACE_ACCOUNT:role/REPLACE_ROLE
automountServiceAccountToken: true
