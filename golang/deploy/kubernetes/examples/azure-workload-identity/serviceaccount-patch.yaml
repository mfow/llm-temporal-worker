apiVersion: v1
kind: ServiceAccount
metadata:
  name: llmtw-worker
  annotations:
    # Replace with the reviewed user-assigned managed identity client ID.
    azure.workload.identity/client-id: REPLACE_CLIENT_ID
automountServiceAccountToken: true
