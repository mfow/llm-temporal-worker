apiVersion: apps/v1
kind: Deployment
metadata:
  name: llmtw-worker
spec:
  template:
    metadata:
      labels:
        azure.workload.identity/use: "true"
    spec:
      automountServiceAccountToken: true
