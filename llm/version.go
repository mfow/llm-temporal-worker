// Package llm contains the provider-neutral semantic API for one inference
// operation.
package llm

// APIVersion identifies the canonical JSON request and response contract.
const APIVersion = "llm.temporal/v1"
