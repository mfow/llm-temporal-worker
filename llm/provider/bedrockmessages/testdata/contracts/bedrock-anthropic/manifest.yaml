version: 1
id: bedrock-anthropic
provider: bedrock
family: messages
coverage: bootstrap
metadata: metadata.yaml
service_classes:
  economy:
    requested_tier: flex
    actual_tier: flex
  standard:
    requested_tier: default
    actual_tier: default
  priority:
    requested_tier: priority
    actual_tier: priority
cases:
  - id: captured-wire-request
    artifacts:
      wire: request.wire.json
  - id: classified-error
    artifacts:
      wire: error.access-denied.json
